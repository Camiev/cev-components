import styled from 'styled-components';

const gray = '#858588';

const style = styled.div`
  .contents {
    color: ${gray};
  }
  .icon-list {
    vertical-align: middle;
  }
  .card {
    margin-top: 1em;
  }
`;

export default style;
